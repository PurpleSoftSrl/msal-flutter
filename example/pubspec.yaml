name: msal_flutter_example
description: Demonstrates how to use the msal_flutter plugin.
version: 2.0.0-alpha.2
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  msal_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
